{
	"description": "Package info - forms the root node of the package",
	"type": "type",
	"id": "package",
	"fields": {
		"aliases": {
			"description": "Map of import aliases used in this package: key = alias, value = package path.",
			"type": "@map",
			"items": {
				"type": "json:@string"
			},
			"optional": true
		},
		"recursive": {
			"description": "Should we scan subdirectories for data files?",
			"type": "json:@bool",
			"optional": true
		}
	}
}
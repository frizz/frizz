description: This represents a gallery - it has a title and a map of images with a restriction rule
type: system:type
id: gallery2a
fields:
  title:
    type: system:@string
  images:
    type: system:@map
    items:
      type: images:@photo
      rules:
        - selector: ".protocol"
          type: system:@string
          equal: https
package frizz

/*
func Validate(value interface{}, imports Importer) (valid bool, message string, err error) {
	val := reflect.ValueOf(value)
	val.Type().PkgPath()
	val.Type().Name()
	if val.Kind() == reflect.Array {
		for i := 0; i < val.Len(); i++ {
			val.Index(i).Interface()
		}
	}
	return false, "", nil
}
*/

//func validate(value interface{}, types )
